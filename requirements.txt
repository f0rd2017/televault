# Runtime dependencies, pinned to the versions verified working in .venv.
# Source of truth for version ranges stays in pyproject.toml; regenerate these
# pins after a dependency bump (pip freeze).
PySide6==6.11.0
QtAwesome==1.4.2
Telethon==1.43.0
python-socks==2.8.1
aiofiles==24.1.0
tenacity==9.1.4
python-dotenv==1.2.2
cryptography==45.0.7
psutil==6.1.1

# Optional Telethon C-speedup present in the working env.
cryptg==0.6.0
